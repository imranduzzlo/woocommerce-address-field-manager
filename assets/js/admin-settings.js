/**
 * WooCommerce Address Field Manager - Admin Settings Script
 * Tabs are now handled via URL parameters (WordPress standard)
 * No JavaScript needed for tab switching - handled by PHP
 */

jQuery(function($) {
	'use strict';

	// No tab switching JavaScript needed
	// Tabs are handled via URL parameters like standard WordPress settings pages
	// Example: admin.php?page=wafm-settings&tab=billing
});
